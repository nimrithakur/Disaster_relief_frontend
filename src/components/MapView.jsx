import React from 'react'

// MapView removed — returning a simple placeholder component to avoid runtime errors
export default function MapView(){
  return (
    <div style={{ padding:12, borderRadius:8, background:'linear-gradient(180deg, rgba(255,255,255,0.01), rgba(255,255,255,0.005))', color:'var(--muted)' }}>
      Map view has been removed.
    </div>
  )
}
